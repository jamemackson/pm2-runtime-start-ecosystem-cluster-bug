{
  "name": "proxy",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start:pm2": "pm2-runtime ts-node --env production --raw --output /dev/stdout --error /dev/stdout -- --transpile-only server.ts"
  },
  "dependencies": {
    "@types/shelljs": "^0.8.8",
    "@types/wait-on": "^5.3.0",
    "shelljs": "^0.8.4",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2",
    "wait-on": "^5.3.0"
  }
}
